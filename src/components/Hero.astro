---
// Hero section component for SUMAQ AGRO
---

<section id="inicio" class="hero">
  <div class="hero-background">
    <div class="overlay"></div>
    <div class="geometric-pattern"></div>
  </div>
  
  <div class="container">
    <div class="hero-content">
      
      <h1 class="hero-title">
        <span class="title-line">Honramos a la madre Naturaleza y transformamos su belleza en propósito
        </span>
      </h1>
      
      
      <div class="hero-actions">
        <a href="/productos" class="btn btn-primary">Conoce Nuestros Productos</a>
        <a href="/contacto" class="btn btn-secondary">Contáctanos</a>
      </div>

    </div>
    
    <div class="hero-visual">
      <div class="floating-elements">
        <div class="element element-1"></div>
        <div class="element element-2"></div>
        <div class="element element-3"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding-top: 100px;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: url('/hero-bg.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, rgba(50, 65, 28, 0.7) 0%, rgba(66, 92, 37, 0.6) 50%, rgba(159, 88, 24, 0.5) 100%);
  }

  .geometric-pattern {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
      radial-gradient(circle at 20% 80%, rgba(176, 137, 25, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(66, 92, 37, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 40% 40%, rgba(159, 88, 24, 0.05) 0%, transparent 50%);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: 3fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero-content {
    max-width: 1400px;
  }

  .hero-badge {
    margin-bottom: 1.5rem;
  }

  .hero-badge span {
    background: linear-gradient(135deg, #B08919 0%, #9F5818 100%);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 25px;
    font-size: 0.9rem;
    font-weight: 600;
    display: inline-block;
  }

  .hero-title {
    font-size: clamp(2.5rem, 7vw, 4rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: #ffffff;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6);
  }

  .title-line {
    display: block;
    background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none;
  }

  .hero-slogan {
    margin-bottom: 2rem;
    text-align: center;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.95);
    border-radius: 15px;
    border-left: 4px solid #B08919;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
  }

  .slogan-text {
    font-size: 1.1rem;
    font-style: italic;
    color: #32411C;
    margin-bottom: 0.5rem;
    font-weight: 500;
  }

  .slogan-author {
    font-size: 0.9rem;
    color: #9F5818;
    font-weight: 600;
  }



  .hero-actions {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 4rem;
    flex-wrap: wrap;
  }

  .btn {
    padding: 1rem 2rem;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    display: inline-block;
    border: 2px solid transparent;
  }

  .btn-primary {
    background: linear-gradient(135deg, #425C25 0%, #9F5818 100%);
    color: white;
    box-shadow: 0 8px 25px rgba(66, 92, 37, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 35px rgba(66, 92, 37, 0.4);
  }

  .btn-secondary {
    background: #425C25;
    color: white;
    border-color: #425C25;
  }

  .btn-secondary:hover {
    
    transform: translateY(-2px);
  }

  .hero-stats {
    display: flex;
    gap: 3rem;
    flex-wrap: wrap;
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    display: block;
    font-size: 2.5rem;
    font-weight: 800;
    color: #9F5818;
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9rem;
    color: #32411C;
    opacity: 0.8;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .hero-visual {
    position: relative;
    height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .floating-elements {
    position: relative;
    width: 400px;
    height: 400px;
  }

  .element {
    position: absolute;
    border-radius: 50%;
    animation: float 6s ease-in-out infinite;
  }

  .element-1 {
    width: 120px;
    height: 120px;
    background: linear-gradient(135deg, #425C25 0%, #32411C 100%);
    top: 20%;
    left: 20%;
    animation-delay: 0s;
  }

  .element-2 {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, #9F5818 0%, #B08919 100%);
    top: 60%;
    right: 20%;
    animation-delay: 2s;
  }

  .element-3 {
    width: 100px;
    height: 100px;
    background: linear-gradient(135deg, #B08919 0%, #9F5818 100%);
    bottom: 20%;
    left: 40%;
    animation-delay: 4s;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px) rotate(0deg);
    }
    50% {
      transform: translateY(-20px) rotate(180deg);
    }
  }

  @media (max-width: 768px) {
    .container {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-stats {
      justify-content: center;
    }

    .hero-visual {
      height: 300px;
    }

    .floating-elements {
      width: 250px;
      height: 250px;
    }

    .hero-slogan {
      text-align: center;
    }
  }
</style>
